#include "main.h"
#include <stdio.h>

/**
 * print_last_digit: prints last digit
 *
 * @num: input number 
 *
 * Return: returns last digit
 */
int print_last_digit(int num)
{
	
	return (num % 10);
}
